function quipu_calculator (){
    //let str = prompt("Enter: ");
    //let array;
   /* let array = str.replace(/[,.""''()?!-]/g, '');   
    str = array.split('*');// "/" ||"-" || "+");
    for(let i=0; i<str.length; i++)
    {
        if(str[i]);
    }*/
    /* while(str.length)
    {
    str = str.replace('~~~', '00');    
    str = str.replace('~~', '0');
    str = str.replace('~', '');
    str = str.replace('@', '1');   
    }
*/
    //str = array.split("~");
  //  console.log(str);
}
